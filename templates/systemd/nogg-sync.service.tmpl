[Unit]
Description=Nogging mechanical Beads to OpenSpec sync ({{NAME}})
After=default.target

[Service]
Type=oneshot
WorkingDirectory={{WORKDIR}}
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart={{WORKDIR}}/scripts/nogg sync
