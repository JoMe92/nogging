# Tasks

- [x] TASK-ACCEPT-001 Write `docs/acceptance.md`: the full ordered procedure from an empty git repository to a signed-off run, every step tagged `[M]` mechanical or `[A]` agent-driven, with its prerequisites, the exact command or action, and the expected observable outcome.
- [x] TASK-ACCEPT-002 Add the canned example change under `scripts/fixtures/acceptance/` — `proposal.md`, `design.md`, `tasks.md` with `TASK-ACCEPTX-001` and `TASK-ACCEPTX-002`, and `specs/acceptance-example/spec.md` with one requirement and one scenario — describing a throwaway "echo note" capability that cannot be mistaken for real product intent.
- [x] TASK-ACCEPT-003 Implement `scripts/acceptance.sh`: create a throwaway repo, install SpecForge from the local checkout (`node bin/cli.js init`), then run `plan-begin`, copy the fixture into `openspec/changes/acceptance-example/`, `validate`, `materialize`, close the first task's Bead, `sync` twice, `doctor`, `audit`, `plan-end`, and tear the repo down; print the `[M]` step tags it covers.
- [x] TASK-ACCEPT-004 Add a `--mechanical` mode to `scripts/acceptance.sh` that stubs `bd` with the same contract as `scripts/specforge.test.sh`, asserts the readiness verdict names the uninitialized tracker as the only gap, asserts one materialize-create call per fixture task, and needs no Beads backend.
- [x] TASK-ACCEPT-005 Assert the sync round-trip in `scripts/acceptance.sh`: the first `sync` flips exactly one `- [ ]` → `- [x]` and appends exactly one `execution-log.md` entry for the closed Bead; the second `sync` reports no changes and leaves no git diff.
- [x] TASK-ACCEPT-006 Add `scripts/acceptance.test.sh` as a thin wrapper that runs `scripts/acceptance.sh --mechanical` and skips with a PASS when `node` is absent, matching `scripts/cli.test.sh`; confirm `scripts/test` discovers and runs it.
- [x] TASK-ACCEPT-007 Add an `acceptance` job to `.github/workflows/specforge-validate.yml` that sets up Node and runs `scripts/acceptance.sh --mechanical`, leaving the `script-tests`, `installer` and `validate` jobs unchanged.
- [x] TASK-ACCEPT-008 Write `docs/acceptance-report-template.md`: toolkit version and git SHA, each runbook step with pass / fail / skipped plus a note, a deviations section, a discoveries section listing Bead IDs, and a trailing `Signed-off-by:` line left blank; document the `docs/acceptance/<date>-<host>.md` naming.
- [x] TASK-ACCEPT-009 Update `docs/operating-model.md` and `README.md` to point to `docs/acceptance.md` and the `docs/acceptance/` report store, and state that a change is not accepted until a signed report exists.
- [ ] TASK-ACCEPT-010 Run the full `[A]` + `[M]` runbook once on the delivery Pi, including the real `npx github:JoMe92/specforge init` step if the repository is pushed and tagged (otherwise record it skipped with the reason); commit the resulting `docs/acceptance/` report and file any failure as a discovery on its Bead rather than fixing it here.
