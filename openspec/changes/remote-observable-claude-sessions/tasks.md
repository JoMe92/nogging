# Tasks

- [x] TASK-SESSION-001 Add session-supervision config to `.specforge/config.json`: dedicated tmux server socket name, `.specforge/state/sessions/` directory, log size cap and rotation depth, stop grace period, and the restricted launch permission-profile path; pick values consistent with existing keys such as `sync_lock_ttl_seconds`.
- [x] TASK-SESSION-002 Implement `session launch --role <r> --bead <id> [--cwd <path>] [--read-only]` in `scripts/specforge` (or a session module it invokes): verify the Bead exists, refuse if a live session already covers the same role+Bead, generate a deterministic collision-free name `sf-<role>-<bead>-<nonce>`, and create the tmux session under the dedicated server socket.
- [x] TASK-SESSION-003 Enforce name-collision safety: check `tmux has-session` and the metadata store before creating a session; on collision, fail or regenerate the nonce and never attach to, reuse, or overwrite an existing session.
- [x] TASK-SESSION-004 Write the durable per-session metadata record (`.specforge/state/sessions/<name>.json`) before exec: `name`, `bead_id`, `role`, `host`, `started_at`, `working_dir`, redacted `command`, `owner`, `state`, `log_path`; update it on every state transition and set `ended_at`/`exit_reason` on stop or failure.
- [x] TASK-SESSION-005 Start an append-only log via `tmux pipe-pane -o` to `<name>.log` beside the metadata record; implement size-based rotation to the configured depth; record `log_path` in the metadata.
- [ ] TASK-SESSION-006 Implement `session list`: read every metadata record, reconcile each against `tmux list-sessions`, report a `running` record with no tmux session as `failed`, and print Bead ID, role, start time, working directory, owner, and state without attaching. Confirm it runs in a bare SSH shell with no `TERM`/tmux assumptions.
- [ ] TASK-SESSION-007 Implement `session attach <name> [--read-only]` (`tmux attach`, `-r` when read-only) and `session log <name> [--follow]` (read/tail the log file, never attach); ensure neither is triggered as a side effect of any other subcommand.
- [ ] TASK-SESSION-008 Implement `session stop <name> [--reason <text>]`: interrupt the Claude process, terminate the pane/session after the grace period, set state `stopped` with `ended_at` and `exit_reason`; make it idempotent.
- [ ] TASK-SESSION-009 Implement `session cleanup [<name>]`: refuse any record in state `starting`, `running` or `idle`; for terminal records remove the lingering tmux session, archive-rotate the log, and mark the record retired; with no name, act only on already-terminal records; optionally `kill-server` when no records remain.
- [ ] TASK-SESSION-010 Define the restricted launch permission profile (no `git push`, no remote Dolt sync, no destructive shell, scoped to the working directory) and the launch wrapper that reads any token from the environment/file rather than argv, sets no shell trace, and redacts known secret-bearing variable names/values from the recorded `command` and the log.
- [ ] TASK-SESSION-011 Ensure `session launch` performs no `bd` mutation and that the launched session prompt/profile states claiming or starting a Bead is a deliberate operator-directed action, not an automatic consequence of launch; specialists still operate only on an already-claimed Bead.
- [ ] TASK-SESSION-012 Update `docs/operating-model.md`, `docs/architecture.md`, `docs/failure-recovery.md`, and `AGENTS.md` to describe tmux-based session supervision, the durable metadata, remote list/attach/log, deliberate stop/cleanup, the no-autonomous-claim rule, and the least-authority/no-credential-logging constraints; reconcile this with the Task-tool specialist framing.
- [ ] TASK-SESSION-013 Add regression tests/fixtures (in `scripts/specforge.test.sh` or a sibling) with a stubbed `tmux`: launch writes metadata and a log before exec; a duplicate name is rejected without overwrite; `list` reports a vanished session as `failed`; `stop` records a terminal state and is idempotent; `cleanup` refuses a running record and succeeds on a stopped one; `launch` issues no `bd` mutation; the recorded command and log contain no seeded secret value.
