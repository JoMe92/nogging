# Tasks

- [ ] TASK-STS-001 Add `repo_sync_state(on_request=False)` to `scripts/specforge`: return `("ok", branch)` or `("skip", reason)`. Skip when a fresh `planning.lock` is held; when `.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` / `CHERRY_PICK_HEAD` / `BISECT_LOG` exists under `git rev-parse --git-dir`; when HEAD is detached (`git symbolic-ref -q --short HEAD` fails); when the branch is in `sync_protected_branches` and `on_request` is False. Otherwise `("ok", branch)`.
- [ ] TASK-STS-002 Add `"sync_protected_branches": ["main", "develop"]` to `.specforge/config.json`; read it with a `["main", "develop"]` default so an un-upgraded config still works.
- [ ] TASK-STS-003 In `sync()`, call `repo_sync_state()` before the sync lock: on `skip`, print `sync: skipped (<reason>)` and return without recording a failure, without touching `last-success.json`, and without backoff. On `ok`, proceed as today and keep the branch for TASK-STS-004.
- [ ] TASK-STS-004 Make `last-success.json` `{"at": <iso>, "branch": <name>}`; after a successful commit, if the branch differs from the stored one, print `sync: note — mirroring on <new> (last run was on <old>)`. Keep reading an old `{"at": …}`-only file gracefully.
- [ ] TASK-STS-005 Have `sync_now()` call `sync()` in a mode where `repo_sync_state(on_request=True)` allows a protected branch but still skips a genuine mid-operation / held planning lock, with a message telling the operator to finish that first.
- [ ] TASK-STS-006 Record a skipped tick so a stalled mirror is visible: write `.specforge/state/last-skip.json` `{"at": <iso>, "reason": <reason>}` on skip (cleared on the next successful or no-op sync), and have `scripts/specforge doctor` report `NOTE  last sync skipped: <reason> (<age>)` when it is present.
- [ ] TASK-STS-007 Add `scripts/specforge.test.sh` coverage (stub `bd`, `SPECFORGE_ROOT` scratch repo): a held fresh planning lock → skip, no failure record, `last-success.json` unchanged; a fake `.git/MERGE_HEAD` → skip; detached HEAD → skip; HEAD on `develop` → timer skip but `sync --now` proceeds; a branch change between runs → the note is printed and `last-success.json.branch` updates; `last-skip.json` written on skip and surfaced by `doctor`.
- [ ] TASK-STS-008 Update `docs/architecture.md` (the sync section — the timer's refusal conditions) and `docs/failure-recovery.md` (a "sync keeps skipping" entry: check `doctor` for the reason; move to a change branch or run `./scripts/specforge sync --now`; `sync_protected_branches` to adjust policy).
