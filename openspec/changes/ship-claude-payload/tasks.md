# Tasks

- [x] TASK-CLD-001 Add `.claude/agents` and `.claude/commands` to the installer verbatim-directory manifest and package allowlist; extend `scripts/cli.test.sh` to verify complete fresh-install delivery, stale-file refresh on `update`, packed-package delivery, and repeat-run idempotence; run `scripts/cli.test.sh` and `scripts/test` successfully.
