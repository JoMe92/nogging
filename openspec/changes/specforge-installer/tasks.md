# Tasks

- [ ] TASK-INSTALLER-001 Add the CLI skeleton (`bin/cli.js` with `init`/`update`/`doctor` and `--force`/`--dry-run`/`--no-hooks`/`--no-systemd`), `bin/lib/manifest.js`, `bin/lib/fsops.js`, and update `package.json` (`bin`, `files`, `engines`, `main`, `description`); verify `node bin/cli.js --help` and `node bin/cli.js init --dry-run` in a temp git repo list the planned files without writing.
- [ ] TASK-INSTALLER-002 Implement the verbatim copy and scaffold-if-absent steps (tool files, `.agents/skills`, `docs/specforge/`, `openspec/config.yaml`, `openspec/project.md` from `templates/openspec-project.md`, `.specforge/config.json` from `templates/specforge-config.json` with `name` = repo basename); verify a temp-repo `init` produces the tool files and that a pre-existing `openspec/project.md` is left untouched.
- [ ] TASK-INSTALLER-003 Implement `bin/lib/merge.js` and its templates: idempotent `.claude/settings.json` PreToolUse guard entry (matcher `Edit|Write`), `.gitignore` line append, and the `<!-- specforge:begin -->`/`<!-- specforge:end -->` block for `CLAUDE.md` and `AGENTS.md`; verify running `init` twice yields exactly one guard entry, no duplicate `.gitignore` lines, and one marker block, with unrelated `settings.json` content preserved.
- [ ] TASK-INSTALLER-004 Implement `bin/lib/systemd.js` + `templates/systemd/*.tmpl` (slugged unit name, `{{WORKDIR}}` absolute path), the post-write steps (chmod, run `scripts/install-hooks`, `core.hooksPath` warning, `.beads/` hint), the `doctor` passthrough, and add a `python3` check to `scripts/specforge` doctor; verify the rendered service has `WorkingDirectory=<tempdir>` and that a repo with a diverted `core.hooksPath` prints the boundary-hooks warning.
- [ ] TASK-INSTALLER-005 Implement the `update` action (skip scaffold, re-apply verbatim + merge + rendered, never touch `openspec/changes/**` or `openspec/project.md` or the config `name`) and write `specforge_version` into `.specforge/config.json` on both `init` and `update`; verify `update` in a temp repo with a fake active change leaves `openspec/changes/` byte-identical and bumps the recorded version.
- [ ] TASK-INSTALLER-006 Add `scripts/cli.test.sh` (guarded by `command -v node`, skip-with-PASS when absent) covering install, idempotent re-`init`, and `update`, and add a `installer` job to `.github/workflows/specforge-validate.yml` running it with Node; verify `scripts/test` passes locally and the new job is defined.
- [ ] TASK-INSTALLER-008 Make `init` run `bd init --init-if-missing --non-interactive` as its first step unless `--no-beads` is given, treating a non-zero exit (e.g. unreachable backend) as a warning rather than an abort; verify a temp-repo `init` creates `.beads/`, a second `init` leaves it untouched, and `--no-beads` skips it.
- [ ] TASK-INSTALLER-009 End `init` and `update` with a readiness verdict: run the `scripts/specforge doctor` checks and print either `SpecForge is ready` or a list of missing prerequisites (tools and an uninitialized tracker); verify the verdict says ready in a fully provisioned temp repo and names the gap after `--no-beads`.
- [ ] TASK-INSTALLER-007 Write `docs/installation.md` (one-line install, file classes touched, update flow, `systemctl --user enable` line, target prerequisites, `core.hooksPath` caveat, `npx github:` auth fallback) and add an "Install into another repo" section to `README.md`; verify the docs render and the install command shown matches `bin/cli.js`.
