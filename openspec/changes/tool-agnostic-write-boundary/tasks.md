# Tasks

- [x] TASK-TWB-001 Add an `openspec.readonly` sentinel under `.specforge/locks/` and a helper in `scripts/specforge` to write/remove it (`{closed_at, by}`); make `plan-begin` remove it and `plan-end` write it, alongside the existing planning-lock acquire/release. Keep `--force` working for both.
- [x] TASK-TWB-002 Ensure a fresh install starts with the sentinel present (created by `scripts/install-hooks` or the first `doctor`/`plan-end`), so `openspec/` is read-only until the first `plan-begin`.
- [x] TASK-TWB-003 Upgrade `scripts/hooks/pre-tool-use-openspec-guard`: block an `openspec/` Edit/Write when the sentinel `.specforge/locks/openspec.readonly` is present, OR when `.specforge/locks/planning.lock` is absent, OR when that lock's `created_at` is older than `planning_lock_ttl_seconds` (read from `.specforge/config.json` — same staleness rule as `lock()`). Closes W6.
- [x] TASK-TWB-004 In the launch path, when the session role is not a planning role and no fresh planning lock is held, add `openspec/` as a read-only root to the session's effective authority: Claude gets `deny` rules for edits under `openspec/**` in its per-session effective-settings file; Codex gets `--sandbox-state-readable-root <repo>/openspec` (verify it denies writes; fall back to an `--add-dir` allowlist or an execpolicy `.rules` patch-path deny per design.md).
- [x] TASK-TWB-005 Confirm `sync()` and ordinary git operations (`switch`/`merge`/`checkout`) are unaffected by the sentinel: `sync()` writes `openspec/` directly as the `sync` writer and needs no lift/restore under the recommended mechanism; add a test that a sync pass mirrors a closed Bead while the sentinel is present.
- [ ] TASK-TWB-006 Add `scripts/specforge doctor` output for the boundary state: `OPEN (planning session active, lock age Ns)` / `LOCKED` / `LOCKED (stale planning lock present — run plan-end --force)`. Read-only, no new exit condition.
- [ ] TASK-TWB-007 Update `docs/architecture.md` "The OpenSpec write boundary": the pre-edit block is a tool-agnostic filesystem/sandbox guard (sentinel + per-session read-only root + staleness-aware PreToolUse fast path) with `pre-commit`/`commit-msg`/CI unchanged below; revise the layer count and the interaction/limitations tables to cover a Codex session. Add a `docs/failure-recovery.md` entry for `openspec/` stuck read-only / stuck writable.
- [ ] TASK-TWB-008 Add regression coverage to `scripts/specforge.test.sh`: guard blocks an `openspec/` write with the sentinel present; guard blocks with a stale planning lock; guard allows during a fresh planning session; `plan-begin`/`plan-end` toggle the sentinel; a launched non-planning session's effective settings carry the `openspec/` read-only rules; `doctor` reports each boundary state.
