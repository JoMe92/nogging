# Tasks

- [x] TASK-SYNC-001 Enumerate closed mapped Beads in the sync pass by adding an explicit closed-status query (or a comma-separated multi-status query) to the Beads read in `scripts/specforge`, so closed mapped Beads reach the existing `status == "closed"` mirror filter.
- [ ] TASK-SYNC-002 Confirm and lock in idempotent mirroring of closed Beads: the `<!-- specforge:<id>:<timestamp> -->` event key and the `- [ ]` → `- [x]` rewrite must produce no diff on a second run; add a closed fixture to `scripts/specforge.test.sh` that runs sync twice and asserts exactly one execution-log entry and one checkbox flip.
- [ ] TASK-SYNC-003 Render enriched execution-log entries containing the Bead ID, the closure timestamp (`closed_at`, falling back to `updated_at`), and the Bead's human-readable note preserved verbatim as an indented block.
- [ ] TASK-SYNC-004 Determine implementation commit references for a closed Bead by scanning `git log` subjects for the Bead ID token and by extracting hex SHAs already present in the Bead note; de-duplicate, render them in the entry, and when none are found state that explicitly.
- [ ] TASK-SYNC-005 Extend the discovery read path so `./scripts/specforge discoveries` enumerates every Bead carrying the `discovery` label regardless of status, sorting blocking first and closed-unreviewed after open.
- [ ] TASK-SYNC-006 Add an acknowledgement ledger under `.specforge/state/` and a planning-session action to record acknowledgements, so acknowledged discoveries stop being surfaced while unacknowledged closed discoveries persist.
- [ ] TASK-SYNC-007 Replace the `.specforge/state/last-error.json` overwrite with a structured active-failure record holding `classification`, `error`, `attempts`, `max_attempts`, `first_seen`, `last_seen`, and `next_retry_after`; clear it on the next successful sync.
- [ ] TASK-SYNC-008 Classify failures mechanically by exception type/source: lock contention and transient `git`/`bd`/JSON errors as `transient` (retry until `max_attempts`), audit/mapping/task-parse failures as `permanent` (no automatic retry); compute `next_retry_after` with capped exponential backoff and add the needed config defaults to `.specforge/config.json`.
- [ ] TASK-SYNC-009 Append every sync failure to a JSONL failure log under `.specforge/state/` and update `./scripts/specforge doctor` to read the new failure record shape and report classification, attempts, and `next_retry_after`.
- [ ] TASK-SYNC-010 Update `docs/architecture.md` and `docs/failure-recovery.md` to describe closed-Bead reconciliation, enriched execution-log entries, closed-discovery review with the acknowledgement ledger, and transient/permanent failure handling with bounded retry.
- [ ] TASK-SYNC-011 Add regression fixtures/tests covering: a closed mapped Bead mirrored exactly once with all enriched fields; a closed discovery surfaced then suppressed after acknowledgement; a transient failure recorded with retry metadata; a permanent failure recorded with no scheduled retry; a successful sync clearing the record.
- [ ] TASK-SYNC-012 Make `materialize()` idempotent across closed Beads: build its already-mapped set from the same closed-inclusive read as the sync pass, so re-running `materialize` for a partly-done change creates no duplicate Bead for a task whose mapped Bead is closed. Add a `scripts/specforge.test.sh` fixture (stub `bd` returning a closed mapped Bead) asserting a second `materialize` creates nothing. Resolves discovery SPEC-dvu.
