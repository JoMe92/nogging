# Tasks

- [x] TASK-LP-001 Add `--profile` and `--prompt` (name-or-path) plus `--full-access` to the `session launch` argparser in `scripts/specforge`, and a `resolve_launch_file(value, dir_key, default_dir, suffix)` helper: return `<dir>/<value><suffix>` when it exists, else treat `value` as a repo-root-relative or absolute path, else raise before any tmux session is created. `--full-access` expands to `--profile trusted --prompt autonomous`.
- [x] TASK-LP-002 Add a fixed `FLOOR_DENY` list in `scripts/specforge` (`rm -rf`/`rm -fr`, `sudo`, `dd`, `mkfs`/`mkfs.*`, `shutdown`, `reboot`, a fork bomb) and a `merge_floor(settings: dict) -> dict` that unions it into `permissions.deny` without dropping existing entries.
- [x] TASK-LP-003 In `session_launch()`, load the resolved profile JSON, run `merge_floor`, write the result to `.specforge/state/sessions/<name>.settings.json`, and pass THAT path (not the source profile) to `scripts/session-launch --settings`. Never mutate a source profile file.
- [x] TASK-LP-004 Create `.specforge/launch-profiles/restricted.json` as a verbatim copy of the current `.specforge/session-launch-profile.json`; make `restricted` the resolved default when `--profile` is absent; keep the `session_launch_profile` config key working (a set value still wins). Do the equivalent for `.specforge/launch-prompts/` and the current `session-launch-prompt.md`.
- [x] TASK-LP-005 Create `.specforge/launch-profiles/trusted.json` (per the design table: `acceptEdits`, `allow` git push/merge/switch/rebase + `bd`/`openspec`/`npx`/`scripts/*`, still deny `sudo`/`systemctl`/network) and `.specforge/launch-prompts/autonomous.md` (per the design "Prompt pairing" section: authorises end-to-end execution of one named change through local fast-forward-merge, keeps every existing hard rule).
- [x] TASK-LP-006 Add `profile` and `effective_settings_path` to the session metadata record; have `session cleanup` / `_archive_log` remove the effective-settings file when the record is retired.
- [x] TASK-LP-007 Add a `PROFILE` column to `session list` (short value: `restricted` shown as `-`, `trusted`, or the basename of a supplied path); keep the row width readable by trimming `WORKING_DIR` if needed.
- [ ] TASK-LP-008 Add `.specforge/launch-profiles/` and `.specforge/launch-prompts/` to `bin/lib/manifest.js` `verbatimDirs` so `npx init`/`update` ship them; confirm `scripts/cli.test.sh` still passes.
- [ ] TASK-LP-009 Document the feature in `docs/operating-model.md` (Session supervision section), `docs/running-work-in-sessions.md` (a "Running with broader authority" subsection covering `--full-access`, what `trusted` grants, the floor, and that `session list` shows it), and `AGENTS.md` (a launched session under `autonomous` may commit/push/merge for its own change only).
- [ ] TASK-LP-010 Add `scripts/session.test.sh` coverage (existing stubs): name resolution, path resolution, unknown-profile fails before any session exists, the floored effective-settings file written and passed as `--settings`, the floor surviving a permissive supplied profile, `--full-access` == `--profile trusted --prompt autonomous`, no-flag default stays `restricted` + no-claim prompt, `session list` shows the profile, `cleanup` removes the effective-settings file.
