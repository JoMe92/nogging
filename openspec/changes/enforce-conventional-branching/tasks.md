# Tasks

- [x] TASK-BRANCH-001 Rewrite the "Branches and commits" section of `docs/operating-model.md` to state the convention precisely: `<type>/<slug>` with `<type>` in {feat, fix, chore, docs, refactor, perf, test, plan}; a change branch's `<slug>` must equal an `openspec/changes/<slug>/` directory name; `chore/<topic>` and `plan/<topic>` for non-change work; `main` and `develop` protected; remove the vague `feat/<change-id>` wording.
- [x] TASK-BRANCH-002 Add `scripts/check-branch-name <ref>`: exit non-zero with an actionable message for a name that does not match `<type>/<slug>`, and for a change branch also assert `openspec/changes/<slug>/` exists in the working tree; treat `main` and `develop` as always valid.
- [x] TASK-BRANCH-003 Add `scripts/hooks/branch-name.test.sh` covering: `feat/<existing-change>` accepted; `plan/<topic>` accepted; `chore/<topic>` accepted; unknown type `wip/x` rejected; change branch with no matching directory rejected; `main` and `develop` accepted; register it so `scripts/test` runs it.
- [x] TASK-BRANCH-004 Teach `scripts/hooks/commit-msg` to treat a `SpecForge-Writer: planning` or `SpecForge-Writer: sync` trailer in the message body as equivalent to `SPECFORGE_WRITER=planning|sync` for the writer exemption; keep the environment variable working; keep the Conventional-subject requirement unconditional.
- [ ] TASK-BRANCH-005 Extend `scripts/hooks/commit-msg.test.sh`: `SpecForge-Writer: planning` trailer exempts a subject with no Beads ID; the trailer still requires a Conventional subject; `SpecForge-Writer: bogus` does not exempt; the `SPECFORGE_WRITER` env exemption still passes.
- [ ] TASK-BRANCH-006 Make the deterministic sync commit in `scripts/specforge` add a `SpecForge-Writer: sync` trailer to its message, and update the planning workflow in `docs/operating-model.md` (and any subject guidance the bridge prints) to use a Conventional subject plus a `SpecForge-Writer: planning` trailer instead of the `plan:` prefix.
- [ ] TASK-BRANCH-007 Add `scripts/hooks/pre-push` that runs `scripts/check-branch-name` for the current branch and blocks a non-conforming push with the shared message; wire it into `scripts/install-hooks` alongside `commit-msg` and `pre-commit`.
- [ ] TASK-BRANCH-008 Add an `invariants` job to `.github/workflows/specforge-validate.yml`, `on: pull_request`: checkout with `fetch-depth: 0`, `git fetch origin ${{ github.base_ref }}`, run `scripts/check-branch-name "${{ github.head_ref }}"`, then for each `git rev-list --no-merges origin/${{ github.base_ref }}..HEAD` commit pipe `git show -s --format=%B` into `scripts/hooks/commit-msg`; fail on the first violation printing the commit SHA and subject.
- [ ] TASK-BRANCH-009 Update `docs/architecture.md` to record the CI `invariants` job as the enforced backstop for branch and commit discipline when `core.hooksPath` diverts local hooks, and to document the `SpecForge-Writer` trailer and its parity with the environment variable.
- [ ] TASK-BRANCH-010 Verify end to end: a badly named branch fails the job; a well-named branch with a commit missing its Beads ID fails naming the SHA; a planning commit carrying the trailer passes; `scripts/test` (including the new branch-name test) is green.
