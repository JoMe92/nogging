# Tasks

- [x] TASK-HY-001 Add `task_map(include_archived=False)` to `scripts/specforge`: when true, after the `changes()` loop also walk `openspec/changes/archive/*/tasks.md`, deriving each change name by stripping a leading `^\d{4}-\d{2}-\d{2}-` from the directory name and recording entries with `"archived": True`; a live task with the same ID always wins.
- [x] TASK-HY-002 Point `validate()` at `task_map(include_archived=True)` and make its checks tolerate archived entries: a closed Bead mapping to an archived task resolves (no "maps missing task", no "change label disagrees"); skip the "checked task must have a closed Bead" reverse check for `archived` task files; do not report a live/archived same-ID pair as a duplicate.
- [x] TASK-HY-003 Regression test in `scripts/specforge.test.sh` (stub `bd`): a fixture change with closed mapped Beads is moved under `openspec/changes/archive/YYYY-MM-DD-<name>/`; `./scripts/specforge validate` stays clean (exit 0, no FAIL lines) and `sync` reports no changes; `materialize` and `sync` still ignore the archived dir (they act only on live changes).
- [x] TASK-HY-004 In `session_records()` exclude any path whose name ends in `.settings.json` (optionally also require the parsed object to carry `name` + `bead_id`); confirm `session list`, both `session cleanup` variants, and the `session_launch` collision guard all benefit.
- [x] TASK-HY-005 Add a `scripts/session.test.sh` case: with a `<name>.settings.json` present beside a real record, `session list` shows only the real session(s) and no `unknown` row; `session cleanup` with no name still stops the tmux server when no real record remains active.
- [x] TASK-HY-006 Remove `"Bash(:(){ :|:& };:)"` from `FLOOR_DENY`; keep the other eight entries.
- [x] TASK-HY-007 Add a `scripts/session.test.sh` assertion that every `FLOOR_DENY` entry is a syntactically valid rule (no empty or nested parentheses — `^Bash\([^()]*\S[^()]*\)$`, `^Bash$`, or `WebFetch`), and that a launched session's effective-settings file contains every `FLOOR_DENY` entry verbatim.
- [ ] TASK-HY-008 Update `docs/failure-recovery.md` and `docs/operating-model.md`: archiving a completed change is supported — its closed Beads keep resolving through the archived `tasks.md`; `archived` changes remain auditable.
