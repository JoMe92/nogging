# Tasks

- [x] TASK-COB-001 Add `mergeCodex(ctx)` to `bin/lib/merge.js` and call it from `applyMerges`: if `.codex/hooks.json` exists, parse and write it back unchanged (a seam for future deep-merge that matches on `hooks.<event>[].command` like `mergeClaudeSettings` does — it must never overwrite the file or drop a `bd`-written entry); assert the shipped `.codex/rules/` and `.codex/prompts/` payload is present and log it. Never touch a user `.codex/AGENTS.md` or `config.toml`.
- [x] TASK-COB-002 Verify the execpolicy `.rules` grammar and file-discovery against the installed Codex, then ship `.codex/rules/specforge.rules` with `deny` rules mirroring the Claude `FLOOR_DENY` + `trusted.json` deny list (`git push`, `git remote`, remote dolt sync, `sudo`, `rm -rf`, `rm -fr`, `dd`, `mkfs`, `shutdown`, `reboot`, `systemctl`, `curl`, `wget`). Add `.codex/rules` and `.codex/prompts` to `bin/lib/manifest.js` `verbatimDirs` and `package.json` `files`.
- [x] TASK-COB-003 Confirm where Codex reads custom prompts (repo `.codex/prompts/` vs `~/.codex/prompts/`), then ship `.codex/prompts/{plan,discovery-review,sync-now}.md` carrying the same persona and step text as `.claude/commands/` (thin wrappers over `scripts/specforge`), adjusted only for the invocation surface.
- [x] TASK-COB-004 Restructure `AGENTS.md` so it is complete and tool-neutral on its own, and add a `## Tool notes` section with labelled **Claude Code** and **Codex** subsections (PreToolUse guard vs filesystem guard + execpolicy; `.claude/agents` Task-tool specialists vs out-of-process `session launch --agent codex --role specialist:<type>`; `.claude/commands` vs `.codex/prompts`).
- [x] TASK-COB-005 Shrink `templates/claude-block.md` to the SpecForge pointer plus the one Claude-only line (the `PreToolUse` hook), dropping the duplicated hard-rule text; give `templates/agents-block.md` the same tool-neutral framing as the restructured `AGENTS.md`; run the installer twice into a temp repo and confirm one marker block and no drift.
- [ ] TASK-COB-006 Document the Codex specialist model in `AGENTS.md` (Tool notes) and `docs/operating-model.md`: a Codex specialist run is a separate supervised `session launch --agent codex --role specialist:<type> --bead <id>` session under every specialist boundary rule; the six `.claude/agents/*.md` stay Claude-only; no MCP.
- [ ] TASK-COB-007 Extend `scripts/specforge doctor`: report `codex` on `PATH` in a reported-but-not-required tier (its absence alone does not make `doctor` exit non-zero); and add an informational `INFO  stale external Codex rule: …` line for any `~/.codex/rules/*.rules` (or project `.rules`) `prefix_rule` whose pattern references a path that does not resolve under `ROOT`.
- [ ] TASK-COB-008 On the installed Codex version, verify whether Codex auto-loads a repo `.agents/skills/**/SKILL.md`, a repo `.codex/skills/`, or neither; then either note it in `docs/using-with-codex.md` (reads `.agents/skills/` or reads neither) or have the installer ship a `.codex/skills/` mirror of `.agents/skills/` (reads `.codex/skills/`). File a discovery on this task's Bead if the outcome needs work beyond a doc line or a copy step.
- [ ] TASK-COB-009 Write `docs/using-with-codex.md`: prerequisites (`codex` CLI + login), what the installer places under `.codex/` (hooks preserved, rules floor, prompts), how `/plan` / `/discovery-review` / `/sync-now` map, the out-of-process specialist model, and the known limitations (no per-tool hook → the boundary is the filesystem guard + execpolicy + the commit hooks). Link it from `docs/installation.md` and `docs/operating-model.md`.
- [ ] TASK-COB-010 Extend `scripts/cli.test.sh`: the packed (`npm pack` extract) install must place `.codex/rules/specforge.rules` and `.codex/prompts/*.md`; `mergeCodex` run twice must be idempotent and must not drop a seeded pre-existing `.codex/hooks.json` `bd` entry; a fresh install must have `AGENTS.md` carrying the `## Tool notes` section.
