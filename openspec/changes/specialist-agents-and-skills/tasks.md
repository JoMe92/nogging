# Tasks

- [x] TASK-AGENT-001 Write `.claude/agents/architect.md`: single responsibility (answer one architecture/design question for a Bead, or a change when the Planning Agent asks), read-only tool scope, output = analysis plus a recommendation handed back to the caller, and the five specialist boundary rules; state explicitly that it never writes `openspec/` or source and is available to both the Lead Agent and the Planning Agent.
- [x] TASK-AGENT-002 Write `.claude/agents/ui-ux-designer.md`: responsibility = turn a UI-facing claimed Bead into an interaction/layout/state specification for the frontend engineer; read-only tool scope; output text is returned to the Lead Agent (kept in the Bead or code comments, never written to `openspec/`); include the boundary rules; Lead-Agent-only.
- [x] TASK-AGENT-003 Write `.claude/agents/backend-engineer.md`: responsibility = implement the server/data/CLI side of exactly one claimed Bead; tool scope includes read, search, edit, and running the build/tests; include the boundary rules (no `openspec/`, one Bead, no claim/close/commit, structured return, discoveries reported up); Lead-Agent-only.
- [ ] TASK-AGENT-004 Write `.claude/agents/frontend-engineer.md`: responsibility = implement the client/UI side of exactly one claimed Bead, consuming any `ui-ux-designer` specification passed in; same tool scope and boundary rules as `backend-engineer`; Lead-Agent-only.
- [ ] TASK-AGENT-005 Write `.claude/agents/code-reviewer.md`: responsibility = review the diff for one claimed Bead against its task slice and spec excerpt, report findings ranked by severity, propose no commits and make no edits; tool scope = read, search, `git diff`; include the boundary rules; Lead-Agent-only.
- [ ] TASK-AGENT-006 Write `.claude/agents/test-runner.md`: responsibility = run the relevant tests for one claimed Bead, add missing coverage for that Bead only, and report pass/fail with the captured output; tool scope = read, search, edit (tests), run tests; include the boundary rules; Lead-Agent-only.
- [ ] TASK-AGENT-007 Add a "Lead Agent delegation" section to `CLAUDE.md` (outside the managed Beads block): after claiming a Bead and reading its task slice, the Lead Agent implements directly or delegates one specialist via the Task tool, passing the Bead ID, task slice and spec excerpt; on return the Lead Agent validates, writes the evidence note, commits with the `[<ID>]` token and closes the Bead; OpenSpec stays read-only; delegate only genuinely isolated work.
- [ ] TASK-AGENT-008 Add a short "Specialist delegation (Claude Code)" subsection to `AGENTS.md` that points to `.claude/agents/` and the `CLAUDE.md` delegation section and states the tool-neutral rule (isolated implementation may be delegated; claiming, closing and committing stay with the Main Worker), without copying in any definition.
- [ ] TASK-AGENT-009 Update `docs/operating-model.md` to describe the delegation model and the Lead Agent knowledge path from section 7 (`bd ready` → claim → `bd show` gives the `openspec:task:<id>` label → targeted read of only that task slice and spec excerpt → decide implement vs delegate → request tests/review → report discovery if needed → `bd close`).
- [ ] TASK-AGENT-010 Add a regression check (in `scripts/specforge.test.sh` or a sibling) that `scripts/specforge materialize` sets both `openspec:change:<name>` and `openspec:task:<TASK-ID>` on every created Bead and that the `openspec:task:<TASK-ID>` label resolves to exactly one `- [ ]` task line in that change's `tasks.md`.
- [ ] TASK-AGENT-011 Add a fixture/test that a delegated specialist context cannot write `openspec/` (the `PreToolUse` guard blocks it) and that the specialist definitions contain no instruction to claim, close, or commit a Bead — a lint over `.claude/agents/*.md` for the forbidden verbs.
