# Tasks

- [x] TASK-ANL-001 Add `--agent {claude,codex}` to the `session launch` argparser and a `session_agent` key to `.specforge/config.json` (default `claude`); `session_launch()` resolves the agent from the flag then the config then the `claude` default, and stores `"agent"` in the metadata record.
- [ ] TASK-ANL-002 Add a `_agent_display(rec)` helper and an `AGENT` column to `session_list()` between `OWNER` and `PROFILE`; a record with no `agent` key shows `claude`.
- [ ] TASK-ANL-003 In `resolved_launch_pair()` / a Codex-aware sibling: for `--agent codex`, resolve the level to `.specforge/launch-profiles/<level>.codex.toml` (name-or-path, `.codex.toml` suffix); a `--profile` pointing at a `.json` file with `--agent codex` is a hard error before any session exists; a supplied `.toml` path is used directly.
- [ ] TASK-ANL-004 Create `.specforge/launch-profiles/restricted.codex.toml` (`sandbox = "workspace-write"`, `ask_for_approval = "on-request"`, `network_access = false`) and `.specforge/launch-profiles/trusted.codex.toml` (`ask_for_approval = "never"`, otherwise the same); document the key set in a comment.
- [ ] TASK-ANL-005 In `session_launch()` for a Codex session: skip the effective-settings JSON write (leave `effective_settings_path` null), read the `.codex.toml` keys, and pass `--agent codex --sandbox <mode> --approval <policy> --network <on|off>` to `scripts/session-launch` alongside `--cwd` / `--bead` / `--prompt`. `--read-only` forces `--sandbox read-only`.
- [ ] TASK-ANL-006 Rewrite `scripts/session-launch` to dispatch on a new `--agent` arg (default `claude`): the `claude` branch is byte-for-byte the current behaviour; the `codex` branch execs `codex --cd <cwd> --sandbox <mode> --ask-for-approval <policy> [-c 'sandbox_workspace_write.network_access=false'] "<prompt text>"`. Never pass `--ignore-rules` or `--dangerously-bypass-approvals-and-sandbox`. No token as an argument.
- [ ] TASK-ANL-007 When `--agent codex` and `.codex/rules/specforge.rules` is absent under the working dir, `session_launch()` prints a warning naming the missing floor file and continues (the floor file ships with `codex-onboarding`).
- [ ] TASK-ANL-008 Make `--full-access` fill `trusted` / `autonomous` per agent (Claude unchanged; Codex → `trusted.codex.toml` + `autonomous` prompt). Ensure `redact()` also scrubs a secret-named value inside a `-c key=value` override before it reaches the recorded `command`.
- [ ] TASK-ANL-009 Add an optional-tools check to `doctor()`: report `codex` present/absent as a note, never a FAIL, never affecting the exit code.
- [ ] TASK-ANL-010 Add `.specforge/launch-profiles/*.codex.toml` to `bin/lib/manifest.js` (the dir is already a `verbatimDir`) and confirm `package.json` `files` still covers it; extend the packed-install check in `scripts/cli.test.sh` to assert the `.codex.toml` fragments ship.
- [ ] TASK-ANL-011 Add `scripts/session.test.sh` coverage with a PATH-stubbed `codex` (logs its argv): `--agent codex` execs `codex` with the mapped `--sandbox`/`--ask-for-approval`; the record shows `agent: codex` and `session list` shows the AGENT column; `--profile <claude.json> --agent codex` fails before a session exists; `--read-only` forces `--sandbox read-only`; a `-c` secret value is redacted in the record; the missing-floor warning fires; the default still execs `claude`.
- [ ] TASK-ANL-012 Update `docs/running-work-in-sessions.md` (a "Choosing the agent" subsection: `--agent codex`, `session_agent`, what `restricted`/`trusted` map to for Codex, the `--sandbox`/approval model, that `session list` shows the agent) and `docs/operating-model.md` (Session supervision: launches are agent-neutral).
